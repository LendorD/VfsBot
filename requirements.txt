# --- Telegram-бот ---
aiogram==3.13.1            # Асинхронный фреймворк для Telegram Bot API (FSM, роутеры)

# --- База данных ---
SQLAlchemy[asyncio]==2.0.35  # ORM с поддержкой async
asyncpg==0.29.0           # Асинхронный драйвер PostgreSQL
alembic==1.13.2           # Миграции схемы БД (опционально)

# --- Планировщик ---
APScheduler==3.10.4       # Периодическая проверка свободных слотов

# --- Браузерная автоматизация ---
# playwright и patchright должны быть совместимы по greenlet/pyee, поэтому
# держим их в одной мажорной линии (диапазоны, а не жёсткие пины).
playwright>=1.55,<2       # Управление браузером (асинхронный API)
# Патченый Playwright: маскирует CDP-детект, помогает проходить Cloudflare.
# Опционально, но рекомендуется. После установки: patchright install chromium
patchright>=1.55,<2

# --- Логирование ---
loguru==0.7.2             # Структурированные логи

# --- Конфигурация ---
pydantic==2.9.2           # Валидация и типизация настроек
pydantic-settings==2.5.2  # Загрузка настроек из .env
python-dotenv==1.0.1      # Чтение .env

# --- Веб-сайт (личный кабинет) ---
fastapi==0.115.0          # Веб-фреймворк
uvicorn==0.30.6           # ASGI-сервер для запуска сайта
jinja2==3.1.4             # Шаблоны страниц
python-multipart==0.0.9   # Разбор данных форм
itsdangerous==2.2.0       # Подпись cookie-сессий

# --- Тесты ---
pytest==8.3.3             # Юнит-тесты
pytest-asyncio==0.24.0    # Поддержка async-тестов
